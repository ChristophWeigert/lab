---
services:
  postgresql:
    image: docker.io/library/postgres:16.9@sha256:3264898409adb05925064daab9158c2d43ff9ad5d8fbcfdb5aad3a937240da7a
    container_name: "authentik_postgresql"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      start_period: 20s
      interval: 30s
      retries: 5
      timeout: 5s
    volumes:
      - database:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?error}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_DB: ${POSTGRES_DB}
      TZ: ${TZ:-UTC}
    networks:
      - internal
  redis:
    image: docker.io/library/redis:8.2.0-alpine@sha256:7521abdff715d396aa482183942f3fe643344287c29ccb66eee16ac08a92190f
    command: --save 60 1 --loglevel warning
    restart: unless-stopped
    container_name: "authentik_redis"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep PONG"]
      start_period: 20s
      interval: 30s
      retries: 5
      timeout: 3s
    networks:
      - internal
    volumes:
      - redis:/data
  server:
    image: ghcr.io/goauthentik/server:2025.6.4@sha256:acde6a1fc963301fe27f7e86facb9aefbc68eb523210bcab5679a899e6bf72d5
    restart: unless-stopped
    command: server
    container_name: "authentik_server"
    environment:
      AUTHENTIK_REDIS__HOST: redis
      AUTHENTIK_POSTGRESQL__HOST: authentik_postgresql
      AUTHENTIK_POSTGRESQL__USER: ${POSTGRES_USER}
      AUTHENTIK_POSTGRESQL__NAME: ${POSTGRES_DB}
      AUTHENTIK_POSTGRESQL__PASSWORD: ${POSTGRES_PASSWORD:?error}
      AUTHENTIK_ERROR_REPORTING__ENABLED: ${AUTHENTIK_ERROR_REPORTING:-false}
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY?error}
      # AUTHENTIK_EMAIL__HOST: ${EMAIL_HOST:?error}
      # AUTHENTIK_EMAIL__USERNAME: ${EMAIL_USERNAME:?error}
      # AUTHENTIK_EMAIL__PASSWORD: ${EMAIL_PASSWORD:?error}
      # AUTHENTIK_EMAIL__FROM: ${EMAIL_FROM:?error}
      AUTHENTIK_EMAIL__PORT: ${EMAIL_PORT:-25}
      AUTHENTIK_EMAIL__USE_TLS: ${EMAIL_USE_TLS:-false}
      AUTHENTIK_EMAIL__USE_SSL: ${EMAIL_USE_SSL:-false}
      AUTHENTIK_EMAIL__TIMEOUT: ${EMAIL_TIMEOUT:-10}
    labels:
      traefik.enable: "true"
      traefik.http.routers.authentik.entrypoints: "websecure"
      traefik.http.routers.authentik.rule: "Host(`authentik.$DOMAIN`) || Host(`authentik.$EXTERNAL_DOMAIN`)"
      traefik.http.routers.authentik.tls: "true"
      traefik.http.routers.authentik.tls.domains[0].main: "$DOMAIN"
      traefik.http.routers.authentik.tls.domains[0].sans: "*.$DOMAIN"
      traefik.http.routers.authentik.tls.domains[1].main: "$EXTERNAL_DOMAIN"
      traefik.http.routers.authentik.tls.domains[1].sans: "*.$EXTERNAL_DOMAIN"
      traefik.http.routers.authentik.tls.certresolver: "production"
      traefik.http.services.authentik.loadbalancer.server.port: "9000"
      traefik.docker.network: "proxy"
    volumes:
      - /mnt/authentik/media:/media
      - /mnt/authentik/custom-templates:/templates
      - /mnt/authentik/custom.css:/web/dist/custom.css:ro
      - /etc/timezone:/etc/timezone:ro
    networks:
      - internal
      - proxy
    depends_on:
      - postgresql
      - redis
  worker:
    image: ghcr.io/goauthentik/server:2025.6.4@sha256:acde6a1fc963301fe27f7e86facb9aefbc68eb523210bcab5679a899e6bf72d5
    restart: unless-stopped
    command: worker
    container_name: "authentik_worker"
    environment:
      AUTHENTIK_REDIS__HOST: redis
      AUTHENTIK_POSTGRESQL__HOST: authentik_postgresql
      AUTHENTIK_POSTGRESQL__USER: ${POSTGRES_USER}
      AUTHENTIK_POSTGRESQL__NAME: ${POSTGRES_DB}
      AUTHENTIK_POSTGRESQL__PASSWORD: ${POSTGRES_PASSWORD:?error}
      AUTHENTIK_ERROR_REPORTING__ENABLED: ${AUTHENTIK_ERROR_REPORTING:-false}
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY?error}
      #AUTHENTIK_EMAIL__HOST: ${EMAIL_HOST:?error}
      #AUTHENTIK_EMAIL__USERNAME: ${EMAIL_USERNAME:?error}
      #AUTHENTIK_EMAIL__PASSWORD: ${EMAIL_PASSWORD:?error}
      #AUTHENTIK_EMAIL__FROM: ${EMAIL_FROM:?error}
      AUTHENTIK_EMAIL__PORT: ${EMAIL_PORT:-25}
      AUTHENTIK_EMAIL__USE_TLS: ${EMAIL_USE_TLS:-false}
      AUTHENTIK_EMAIL__USE_SSL: ${EMAIL_USE_SSL:-false}
      AUTHENTIK_EMAIL__TIMEOUT: ${EMAIL_TIMEOUT:-10}
    volumes:
      - /mnt/authentik/media:/media
      - /mnt/authentik/certs:/certs
      - /mnt/authentik/custom-templates:/templates
      - /mnt/authentik/custom.css:/web/dist/custom.css:ro
      - /etc/timezone:/etc/timezone:ro
    networks:
      - internal
    depends_on:
      - postgresql
      - redis

volumes:
  database:
    driver: local
  redis:
    driver: local

networks:
  proxy:
    external: true
  internal: