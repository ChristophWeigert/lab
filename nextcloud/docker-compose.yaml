---
volumes:
  nextcloud_config:
    external: true
  nextcloud_data:
    external: true

networks:
  proxy:
    external: true

services:
  nextcloud:
    container_name: "nextcloud"
    environment:
      - "NEXTCLOUD_PATH=/config/www/nextcloud"
    hostname: "nextcloud"
    image: "linuxserver/nextcloud:28.0.4@sha256:e05bf99c84c981383320e79863052c7ccadb9a8ee03d8477488cb991ee509a44"
    labels:
      traefik.enable: "true"
      traefik.http.routers.nextcloud.entrypoints: "websecure"
      traefik.http.routers.nextcloud.rule: "Host(`$DOMAIN`) || Host(`$EXTERNAL_DOMAIN`)"
      traefik.http.routers.nextcloud.tls: "true"
      traefik.http.routers.nextcloud.tls.certresolver: "production"
      traefik.docker.network: "proxy"
    networks:
      - proxy
    restart: "always"
    volumes:
      - "nextcloud_config:/config"
      - "nextcloud_data:/data"
    working_dir: "/"
