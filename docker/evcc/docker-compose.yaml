networks:
  proxy:
    external: true

volumes:
  evcc_data: {}

services:
  evcc:
    command:
      - evcc
    container_name: evcc
    networks: 
      - proxy
    image: evcc/evcc:0.315.0@sha256:327318279998448d81fad5c2ffe24e1125d58940170bff73162ec8e3796fabdb
    ports:
      - 4712:4712/tcp
    volumes:
      #- /mnt/evcc/evcc.yaml:/etc/evcc.yaml
      - evcc_data:/root/.evcc
      - /etc/machine-id:/etc/machine-id:ro
      - /var/lib/dbus/machine-id:/var/lib/dbus/machine-id:ro
    labels:
        - "traefik.enable=true"
        - "traefik.docker.network=proxy"
        - "traefik.http.routers.evcc.entryPoints=websecure"
        - "traefik.http.routers.evcc.tls.certResolver=production"
        - "traefik.http.routers.evcc.rule=Host(`evcc.$DOMAIN`)"
        - "traefik.http.services.evcc.loadbalancer.server.port=7070"
        - "traefik.http.routers.evcc.tls.domains[0].main=$DOMAIN"
        - "traefik.http.routers.evcc.tls.domains[0].sans=*.$DOMAIN"
        - "traefik.http.routers.evcc.tls=true"
    restart: unless-stopped