networks:
  proxy:
    external: true

volumes:
  zigbee2mqtt:

services:
  zigbee2mqtt:
    container_name: zigbee2mqtt
    image: ghcr.io/koenkk/zigbee2mqtt:2.11.0@sha256:f1f5195076d771727e9ff3db638de0f54d37969a66f317cd34624a7af5faf364
    restart: unless-stopped
    networks:
      - proxy
    volumes:
      - zigbee2mqtt:/app/data
      - /run/udev:/run/udev:ro
    environment:
      - TZ=Europe/Berlin
    labels:
      traefik.enable: "true"
      traefik.http.routers.zigbee2mqtt.entrypoints: "websecure"
      traefik.http.routers.zigbee2mqtt.rule: "Host(`zigbee2mqtt.$DOMAIN`)"
      traefik.http.routers.zigbee2mqtt.tls: "true"
      traefik.http.routers.zigbee2mqtt.tls.certresolver: "production"
      traefik.http.routers.zigbee2mqtt.tls.domains[0].main: "$DOMAIN"
      traefik.http.routers.zigbee2mqtt.tls.domains[0].sans: "*.$DOMAIN"
      traefik.http.services.zigbee2mqtt.loadbalancer.server.port: 8080
      traefik.docker.network: "proxy"