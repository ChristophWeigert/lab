---
volumes:
  data:

networks:
  proxy:
    external: true

services:
  uptime-kuma:
    container_name: uptime-kuma
    image: louislam/uptime-kuma:1.23.14-alpine@sha256:93601e91de38668c054e3017126b3ece4abc601fba09556fec52f6a0ed4aab6c
    environment:
      - TZ=Europe/Berlin
    volumes:
      - data:/app/data
    restart: unless-stopped
    networks:
      - proxy
    labels:
      traefik.enable: "true"
      traefik.http.routers.uptime-kuma.entrypoints: "websecure"
      traefik.http.routers.uptime-kuma.rule: "Host(`$DOMAIN`) || Host(`$EXTERNAL_DOMAIN`)"
      traefik.http.routers.uptime-kuma.tls: "true"
      traefik.http.routers.uptime-kuma.tls.certresolver: "production"
      traefik.docker.network: "proxy"
