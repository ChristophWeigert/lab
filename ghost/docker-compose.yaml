---
volumes:
  ghost_data:
    external: true

networks:
  proxy:
    external: true

services:
  ghost:
    environment:
      - "url=$DOMAIN_GHOST"
      - "database__client=sqlite3"
      - "database__connection__filename=/var/lib/ghost/content/data/ghost.db"
      - "NODE_ENV=production"
      - "GHOST_INSTALL=/var/lib/ghost"
      - "GHOST_CONTENT=/var/lib/ghost/content"
    image: "ghost:5.130.0@sha256:9a716435584b2dd9929cdbc413331dc09d56e9c659de9bd4bcf23cc498b55de2"
    networks:
      - proxy
    labels:
      traefik.enable: "true"
      traefik.http.routers.ghost.entrypoints: "websecure"
      traefik.http.routers.ghost.rule: "Host(`$HOST`)"
      traefik.http.routers.ghost.tls: "true"
      traefik.http.routers.ghost.tls.domains[0].main: "$DOMAIN"
      traefik.http.routers.ghost.tls.domains[0].sans: "*.$DOMAIN"
      traefik.http.routers.ghost.tls.certresolver: "production"
      traefik.docker.network: "proxy"
    restart: "always"
    volumes:
      - "ghost_data:/var/lib/ghost/content"
    working_dir: "/var/lib/ghost"
