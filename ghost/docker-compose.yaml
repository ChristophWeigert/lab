version: "3.6"

services:
  ghost-christophweigertcom:
    container_name: "ghost-christophweigertcom"
    environment:
      - "url=http://christophweigert.com"
      - "database__client=sqlite3"
      - "database__connection__filename=/var/lib/ghost/content/data/ghost.db"
      - "NODE_ENV=production"
      - "GHOST_INSTALL=/var/lib/ghost"
      - "GHOST_CONTENT=/var/lib/ghost/content"
    hostname: "ghostchristophweigertcom"
    image: "ghost:5.61.1"
    network_mode: "bridge"
    labels:
      traefik.enable: "true"
      traefik.http.routers.ghost-christophweigertcom.entrypoints: "websecure"
      traefik.http.routers.ghost-christophweigertcom.rule: "Host(`christophweigert.com`)"
      traefik.http.routers.ghost-christophweigertcom.tls: "true"
      traefik.http.routers.ghost-christophweigertcom.tls.certresolver: "production"
    restart: "always"
    volumes:
      - "ghost_data:/var/lib/ghost/content"
    working_dir: "/var/lib/ghost"

volumes:
  ghost_data:
    external: true
